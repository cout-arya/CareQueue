name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy Placeholder
        run: echo "Replace this with actual deploy steps (e.g., to Render, AWS, or Railway)"
